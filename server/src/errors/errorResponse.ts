// Custom error response class to manually create error responses

class ErrorResponse extends Error {
  statusCode: number;

  constructor(message: string, statusCode: number = 500) {
    super(message);
    this.statusCode = statusCode;
  }
}

export default ErrorResponse;
